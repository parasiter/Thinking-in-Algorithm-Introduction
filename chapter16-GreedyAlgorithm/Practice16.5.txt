16.5-1 对图16-7给出的调度问题的实例，将每个惩罚值wi替换为80-wi,求解修改后的问题。
相当于求最大化惩罚问题，将wi按逆序排即可，其解为：<a7,a6,a4,a3,a5,a2,a1>

16.5-2 说明如何利用引理16.12的性质2在O（|A|）时间内确定一个给定任务集合A是独立的。
实际上，代码的运行过程只需要O（|A|）的时间。因为每加入一个元素X,可以确定其截止时间t，然后遍历所有|A|-1的元素，判断Nt(A)<=t是否成立即可。实际运行时间就是O（|A|）

